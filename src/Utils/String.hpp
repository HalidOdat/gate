#pragma once

#include "Core/Type.hpp"

namespace Gate {
  
  namespace Utils {
    
    bool stringIsEqualIgnoreCase(const StringView& rhs, const StringView& lhs);

  } // namespace Utils
  

} // namespace Gate
